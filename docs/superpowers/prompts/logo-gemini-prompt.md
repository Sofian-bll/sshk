# sshk — Logo Prompt for Gemini

Copy this into Gemini (gemini.google.com). Settings: 2K, 1:1. Send me the PNG. Resize to 256x256 before placing — 2K for quality, 256px for web.

---

Generate a macOS app icon for sshk.

Style: Apple design language. Minimal, clean, one single element.
Zero clutter, zero decoration.

The mark: A terminal cursor/prompt (▋) combined with a key shape. The cursor and key form a single unified geometric mark. Thick geometric lines, clean angles. The cursor suggests the command line, the key suggests SSH security. They merge into one symbol — a key whose bow (top) is shaped like a blinking terminal cursor.

Colors: A solid vibrant green (#86e040), applied to the mark only.
The mark sits centered inside a dark rounded square (#0a0908).
Nothing else. No frame, no glow, no additional shapes.

Background: fully transparent outside the rounded square.
No text, no words, no decorations of any kind.
Size: 2K, ratio 1:1, PNG with alpha channel.
